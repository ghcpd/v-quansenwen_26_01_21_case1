# PrettyTable `add_rows` example project

This is a small, self-contained Python project that implements a tiny subset of
PrettyTable focused on `PrettyTable.add_rows()`.

## Python

- Target: Python 3.10+

## Setup

```
python -m venv .venv
pip install -r requirements.txt
```

## Run tests

```
pytest -q
```
